Apply the Fabric `create_story_explanation` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_story_explanation/system.md

Apply those instructions to the following input:

$ARGUMENTS
