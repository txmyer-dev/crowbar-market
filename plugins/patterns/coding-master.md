Apply the Fabric `coding_master` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/coding_master/system.md

Apply those instructions to the following input:

$ARGUMENTS
