Apply the Fabric `rate_content` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/rate_content/system.md

Apply those instructions to the following input:

$ARGUMENTS
