Apply the Fabric `fix_typos` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/fix_typos/system.md

Apply those instructions to the following input:

$ARGUMENTS
