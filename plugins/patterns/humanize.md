Apply the Fabric `humanize` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/humanize/system.md

Apply those instructions to the following input:

$ARGUMENTS
