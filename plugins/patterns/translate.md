Apply the Fabric `translate` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/translate/system.md

Apply those instructions to the following input:

$ARGUMENTS
