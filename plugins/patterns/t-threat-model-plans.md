Apply the Fabric `t_threat_model_plans` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/t_threat_model_plans/system.md

Apply those instructions to the following input:

$ARGUMENTS
