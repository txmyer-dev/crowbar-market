Apply the Fabric `extract_core_message` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_core_message/system.md

Apply those instructions to the following input:

$ARGUMENTS
