Apply the Fabric `t_analyze_challenge_handling` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/t_analyze_challenge_handling/system.md

Apply those instructions to the following input:

$ARGUMENTS
