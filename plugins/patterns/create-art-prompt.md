Apply the Fabric `create_art_prompt` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_art_prompt/system.md

Apply those instructions to the following input:

$ARGUMENTS
