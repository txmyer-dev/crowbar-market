Apply the Fabric `analyze_answers` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_answers/system.md

Apply those instructions to the following input:

$ARGUMENTS
