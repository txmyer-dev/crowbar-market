Apply the Fabric `improve_prompt` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/improve_prompt/system.md

Apply those instructions to the following input:

$ARGUMENTS
