Apply the Fabric `extract_product_features` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_product_features/system.md

Apply those instructions to the following input:

$ARGUMENTS
