Apply the Fabric `create_pattern` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_pattern/system.md

Apply those instructions to the following input:

$ARGUMENTS
