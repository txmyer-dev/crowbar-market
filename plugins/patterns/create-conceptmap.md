Apply the Fabric `create_conceptmap` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_conceptmap/system.md

Apply those instructions to the following input:

$ARGUMENTS
