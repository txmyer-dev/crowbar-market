Apply the Fabric `write_latex` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/write_latex/system.md

Apply those instructions to the following input:

$ARGUMENTS
