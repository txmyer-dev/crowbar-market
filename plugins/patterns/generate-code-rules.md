Apply the Fabric `generate_code_rules` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/generate_code_rules/system.md

Apply those instructions to the following input:

$ARGUMENTS
