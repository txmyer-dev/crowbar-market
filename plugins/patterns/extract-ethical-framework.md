Apply the Fabric `extract_ethical_framework` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_ethical_framework/system.md

Apply those instructions to the following input:

$ARGUMENTS
