Apply the Fabric `write_hackerone_report` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/write_hackerone_report/system.md

Apply those instructions to the following input:

$ARGUMENTS
