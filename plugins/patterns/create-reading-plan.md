Apply the Fabric `create_reading_plan` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_reading_plan/system.md

Apply those instructions to the following input:

$ARGUMENTS
