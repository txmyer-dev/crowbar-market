Apply the Fabric `create_user_story` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_user_story/system.md

Apply those instructions to the following input:

$ARGUMENTS
