Apply the Fabric `transcribe_minutes` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/transcribe_minutes/system.md

Apply those instructions to the following input:

$ARGUMENTS
