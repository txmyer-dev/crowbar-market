Apply the Fabric `detect_mind_virus` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/detect_mind_virus/system.md

Apply those instructions to the following input:

$ARGUMENTS
