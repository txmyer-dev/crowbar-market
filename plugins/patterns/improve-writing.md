Apply the Fabric `improve_writing` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/improve_writing/system.md

Apply those instructions to the following input:

$ARGUMENTS
