Apply the Fabric `t_check_metrics` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/t_check_metrics/system.md

Apply those instructions to the following input:

$ARGUMENTS
