Apply the Fabric `summarize_prompt` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/summarize_prompt/system.md

Apply those instructions to the following input:

$ARGUMENTS
