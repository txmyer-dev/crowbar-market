Apply the Fabric `create_logo` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_logo/system.md

Apply those instructions to the following input:

$ARGUMENTS
