Apply the Fabric `extract_song_meaning` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_song_meaning/system.md

Apply those instructions to the following input:

$ARGUMENTS
