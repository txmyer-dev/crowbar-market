Apply the Fabric `create_npc` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_npc/system.md

Apply those instructions to the following input:

$ARGUMENTS
