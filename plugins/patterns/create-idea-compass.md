Apply the Fabric `create_idea_compass` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_idea_compass/system.md

Apply those instructions to the following input:

$ARGUMENTS
