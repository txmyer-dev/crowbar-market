Apply the Fabric `create_show_intro` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_show_intro/system.md

Apply those instructions to the following input:

$ARGUMENTS
