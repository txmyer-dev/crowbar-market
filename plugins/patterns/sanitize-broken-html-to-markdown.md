Apply the Fabric `sanitize_broken_html_to_markdown` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/sanitize_broken_html_to_markdown/system.md

Apply those instructions to the following input:

$ARGUMENTS
