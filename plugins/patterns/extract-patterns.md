Apply the Fabric `extract_patterns` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_patterns/system.md

Apply those instructions to the following input:

$ARGUMENTS
