Apply the Fabric `create_report_finding` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_report_finding/system.md

Apply those instructions to the following input:

$ARGUMENTS
