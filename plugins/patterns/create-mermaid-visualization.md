Apply the Fabric `create_mermaid_visualization` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_mermaid_visualization/system.md

Apply those instructions to the following input:

$ARGUMENTS
