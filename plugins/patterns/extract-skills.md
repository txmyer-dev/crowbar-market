Apply the Fabric `extract_skills` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_skills/system.md

Apply those instructions to the following input:

$ARGUMENTS
