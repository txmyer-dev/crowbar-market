Apply the Fabric `extract_algorithm_update_recommendations` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_algorithm_update_recommendations/system.md

Apply those instructions to the following input:

$ARGUMENTS
