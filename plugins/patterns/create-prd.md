Apply the Fabric `create_prd` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_prd/system.md

Apply those instructions to the following input:

$ARGUMENTS
