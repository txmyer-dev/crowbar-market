Apply the Fabric `t_check_dunning_kruger` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/t_check_dunning_kruger/system.md

Apply those instructions to the following input:

$ARGUMENTS
