Apply the Fabric `summarize_git_changes` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/summarize_git_changes/system.md

Apply those instructions to the following input:

$ARGUMENTS
