Apply the Fabric `analyze_paper` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_paper/system.md

Apply those instructions to the following input:

$ARGUMENTS
