Apply the Fabric `extract_mcp_servers` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_mcp_servers/system.md

Apply those instructions to the following input:

$ARGUMENTS
