Apply the Fabric `extract_poc` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_poc/system.md

Apply those instructions to the following input:

$ARGUMENTS
