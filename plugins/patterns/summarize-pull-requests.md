Apply the Fabric `summarize_pull-requests` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/summarize_pull-requests/system.md

Apply those instructions to the following input:

$ARGUMENTS
