Apply the Fabric `analyze_bill` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_bill/system.md

Apply those instructions to the following input:

$ARGUMENTS
