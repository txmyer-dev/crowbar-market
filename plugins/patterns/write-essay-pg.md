Apply the Fabric `write_essay_pg` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/write_essay_pg/system.md

Apply those instructions to the following input:

$ARGUMENTS
