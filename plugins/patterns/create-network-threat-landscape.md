Apply the Fabric `create_network_threat_landscape` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_network_threat_landscape/system.md

Apply those instructions to the following input:

$ARGUMENTS
