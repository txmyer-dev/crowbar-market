Apply the Fabric `create_flash_cards` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_flash_cards/system.md

Apply those instructions to the following input:

$ARGUMENTS
