Apply the Fabric `extract_ideas` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_ideas/system.md

Apply those instructions to the following input:

$ARGUMENTS
