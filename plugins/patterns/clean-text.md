Apply the Fabric `clean_text` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/clean_text/system.md

Apply those instructions to the following input:

$ARGUMENTS
