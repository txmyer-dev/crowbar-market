Apply the Fabric `create_command` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_command/system.md

Apply those instructions to the following input:

$ARGUMENTS
