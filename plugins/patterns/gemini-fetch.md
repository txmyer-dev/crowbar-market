Fetch content from a URL or search a topic using Gemini CLI. This is for sites that Claude can't access directly (Reddit, X/Twitter, etc).

Usage: /gemini-fetch [URL or topic]

Invoke the GeminiResearch skill with the GeminiFetch workflow for the provided URL or topic.
