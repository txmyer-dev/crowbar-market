Apply the Fabric `create_keynote` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_keynote/system.md

Apply those instructions to the following input:

$ARGUMENTS
