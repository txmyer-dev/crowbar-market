Apply the Fabric `write_pull-request` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/write_pull-request/system.md

Apply those instructions to the following input:

$ARGUMENTS
