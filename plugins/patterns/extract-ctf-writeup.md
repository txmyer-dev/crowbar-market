Apply the Fabric `extract_ctf_writeup` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_ctf_writeup/system.md

Apply those instructions to the following input:

$ARGUMENTS
