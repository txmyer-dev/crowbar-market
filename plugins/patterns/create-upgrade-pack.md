Apply the Fabric `create_upgrade_pack` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_upgrade_pack/system.md

Apply those instructions to the following input:

$ARGUMENTS
