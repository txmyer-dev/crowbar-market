Apply the Fabric `create_threat_scenarios` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_threat_scenarios/system.md

Apply those instructions to the following input:

$ARGUMENTS
