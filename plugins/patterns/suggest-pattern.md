Apply the Fabric `suggest_pattern` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/suggest_pattern/system.md
Also read the user context at: ~/.claude/skills/Fabric/Patterns/suggest_pattern/user.md

Apply both the system instructions and user context to the following input:

$ARGUMENTS
