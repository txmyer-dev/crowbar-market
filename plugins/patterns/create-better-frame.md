Apply the Fabric `create_better_frame` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_better_frame/system.md

Apply those instructions to the following input:

$ARGUMENTS
