Apply the Fabric `create_tags` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_tags/system.md

Apply those instructions to the following input:

$ARGUMENTS
