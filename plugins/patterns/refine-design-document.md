Apply the Fabric `refine_design_document` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/refine_design_document/system.md

Apply those instructions to the following input:

$ARGUMENTS
