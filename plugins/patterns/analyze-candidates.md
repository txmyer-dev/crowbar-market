Apply the Fabric `analyze_candidates` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_candidates/system.md

Apply those instructions to the following input:

$ARGUMENTS
