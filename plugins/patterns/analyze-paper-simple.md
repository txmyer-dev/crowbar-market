Apply the Fabric `analyze_paper_simple` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_paper_simple/system.md

Apply those instructions to the following input:

$ARGUMENTS
