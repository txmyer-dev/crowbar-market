Apply the Fabric `apply_ul_tags` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/apply_ul_tags/system.md

Apply those instructions to the following input:

$ARGUMENTS
