Apply the Fabric `extract_latest_video` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_latest_video/system.md

Apply those instructions to the following input:

$ARGUMENTS
