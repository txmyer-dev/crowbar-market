Apply the Fabric `create_prediction_block` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_prediction_block/system.md

Apply those instructions to the following input:

$ARGUMENTS
