Apply the Fabric `create_ttrc_graph` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_ttrc_graph/system.md

Apply those instructions to the following input:

$ARGUMENTS
