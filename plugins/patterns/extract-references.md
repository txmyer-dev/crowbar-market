Apply the Fabric `extract_references` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_references/system.md

Apply those instructions to the following input:

$ARGUMENTS
