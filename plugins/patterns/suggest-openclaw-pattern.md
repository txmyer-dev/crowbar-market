Apply the Fabric `suggest_openclaw_pattern` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/suggest_openclaw_pattern/system.md

Apply those instructions to the following input:

$ARGUMENTS
