Apply the Fabric `analyze_presentation` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_presentation/system.md

Apply those instructions to the following input:

$ARGUMENTS
