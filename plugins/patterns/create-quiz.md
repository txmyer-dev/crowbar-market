Apply the Fabric `create_quiz` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_quiz/system.md

Apply those instructions to the following input:

$ARGUMENTS
