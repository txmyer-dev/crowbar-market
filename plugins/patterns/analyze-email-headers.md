Apply the Fabric `analyze_email_headers` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_email_headers/system.md

Apply those instructions to the following input:

$ARGUMENTS
