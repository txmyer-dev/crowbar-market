Apply the Fabric `extract_sponsors` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_sponsors/system.md

Apply those instructions to the following input:

$ARGUMENTS
