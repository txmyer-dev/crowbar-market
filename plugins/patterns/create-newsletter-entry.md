Apply the Fabric `create_newsletter_entry` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_newsletter_entry/system.md

Apply those instructions to the following input:

$ARGUMENTS
