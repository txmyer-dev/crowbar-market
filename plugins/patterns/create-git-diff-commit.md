Apply the Fabric `create_git_diff_commit` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_git_diff_commit/system.md

Apply those instructions to the following input:

$ARGUMENTS
