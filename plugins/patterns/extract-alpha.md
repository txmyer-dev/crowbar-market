Apply the Fabric `extract_alpha` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_alpha/system.md

Apply those instructions to the following input:

$ARGUMENTS
