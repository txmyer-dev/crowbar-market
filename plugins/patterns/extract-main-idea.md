Apply the Fabric `extract_main_idea` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_main_idea/system.md

Apply those instructions to the following input:

$ARGUMENTS
