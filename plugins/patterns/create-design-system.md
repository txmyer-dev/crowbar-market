Apply the Fabric `create_design_system` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_design_system/system.md

Apply those instructions to the following input:

$ARGUMENTS
