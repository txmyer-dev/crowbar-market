Apply the Fabric `concall_summary` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/concall_summary/system.md

Apply those instructions to the following input:

$ARGUMENTS
