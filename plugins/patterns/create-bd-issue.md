Apply the Fabric `create_bd_issue` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_bd_issue/system.md

Apply those instructions to the following input:

$ARGUMENTS
