Apply the Fabric `create_coding_feature` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_coding_feature/system.md

Apply those instructions to the following input:

$ARGUMENTS
