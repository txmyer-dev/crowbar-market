Apply the Fabric `get_wow_per_minute` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/get_wow_per_minute/system.md

Apply those instructions to the following input:

$ARGUMENTS
