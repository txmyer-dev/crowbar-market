Apply the Fabric `extract_primary_solution` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_primary_solution/system.md

Apply those instructions to the following input:

$ARGUMENTS
