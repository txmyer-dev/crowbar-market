Apply the Fabric `rate_value` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/rate_value/system.md

Apply those instructions to the following input:

$ARGUMENTS
