Apply the Fabric `analyze_terraform_plan` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_terraform_plan/system.md

Apply those instructions to the following input:

$ARGUMENTS
