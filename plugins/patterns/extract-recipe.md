Apply the Fabric `extract_recipe` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/extract_recipe/system.md

Apply those instructions to the following input:

$ARGUMENTS
