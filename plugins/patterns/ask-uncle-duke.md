Apply the Fabric `ask_uncle_duke` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/ask_uncle_duke/system.md

Apply those instructions to the following input:

$ARGUMENTS
