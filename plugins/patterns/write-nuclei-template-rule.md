Apply the Fabric `write_nuclei_template_rule` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/write_nuclei_template_rule/system.md

Apply those instructions to the following input:

$ARGUMENTS
