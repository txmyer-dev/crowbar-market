Apply the Fabric `analyze_prose` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/analyze_prose/system.md

Apply those instructions to the following input:

$ARGUMENTS
