Apply the Fabric `suggest_gt_command` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/suggest_gt_command/system.md

Apply those instructions to the following input:

$ARGUMENTS
