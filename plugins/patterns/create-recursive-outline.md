Apply the Fabric `create_recursive_outline` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/create_recursive_outline/system.md

Apply those instructions to the following input:

$ARGUMENTS
