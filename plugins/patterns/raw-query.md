Apply the Fabric `raw_query` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/raw_query/system.md

Apply those instructions to the following input:

$ARGUMENTS
