Apply the Fabric `threshold` pattern to the provided content.

Read the pattern instructions at: ~/.claude/skills/Fabric/Patterns/threshold/system.md

Apply those instructions to the following input:

$ARGUMENTS
